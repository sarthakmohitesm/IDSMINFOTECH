# Maps JavaScript API — enables styled map on Contact (see OfficeLocationMap.jsx).
# Create a key at https://console.cloud.google.com/ and enable "Maps JavaScript API".
VITE_GOOGLE_MAPS_API_KEY=

# Express API base URL (no trailing slash). Contact form POSTs to /api/contact.
VITE_API_URL=http://localhost:5000
